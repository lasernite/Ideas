<% @notifications.each do |notification| %>
	<div class="notification">
		<%= link_to 'Somebody responded to "' + notification.post_string + '..."' , post_url(Post.find_by(id:notification.post_id)) + '/comments' %>
	</div>
<% end%>